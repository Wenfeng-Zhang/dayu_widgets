#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""Setuptools 构建入口。

所有元数据与包发现规则在 setup.cfg / pyproject.toml 中维护，本文件只做转发。
这样 IDE 与 `pip install -e .` 都能找到标准入口。
"""
# Import built-in modules
from setuptools import setup


setup()
